---
import { urlFor } from "@/lib/sanityImage";

const { node } = Astro.props as { node: { alt?: string; asset?: unknown; [key: string]: unknown } };
const src = node.asset ? urlFor(node).width(1200).auto("format").url() : null;
---

{src && <img src={src} alt={node.alt || ""} loading="lazy" />}
